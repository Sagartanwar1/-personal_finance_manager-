import React from "react";
import Layout from "../components/Layout";

export default function DashboardPage() {
  return (
    <Layout>
      <h1>Dashboard</h1>
      <p>Preview dashboard (charts demo served by static frontend). Use the Transactions page to view/add transactions.</p>
    </Layout>
  );
}